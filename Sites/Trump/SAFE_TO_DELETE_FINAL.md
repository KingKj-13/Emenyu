# Safe To Delete Final

Generated: 2026-05-19

## Verified Removals Completed In This Pass

These files were checked for active references with `rg` and are safe to remove from the active Trump Prime Grillhouse runtime:

- `waiter.js` - legacy root waiter script. The active waiter page loads `frontend/scripts/waiter-app.js`.
- `menu-data.json` - stale scaffold data; no active frontend, backend, chatbot, or recommendation reference.
- `server-local.log`, `server-local.err.log`, `server-3013.log`, `server-3013.err.log` - generated local server logs.
- `temp_speech.mp3` - legacy generated audio artifact; current local TTS writes text output under `Data/speech.txt`.
- `__pycache__/` - generated by Python compilation validation and removed after the check.

## Current Generated Files Kept

- `validation-server.log` and `validation-server.err.log` are tied to the currently running validation server on port `3012`.
- Fresh final screenshots are kept as validation evidence.

## Preserved Deliberately

- `ChatBot.py`, `recommend.py`, `pop_recommend.py`, `josh_main.py`, `tts.py`, and `stt.py` remain because they are local-only AI and voice support files.
- `food/TrumpMenu.json` remains as a regenerated compatibility snapshot for legacy Python loaders and any static consumers.
- `orders/`, `history/`, `tables/`, and `data/` runtime data remain intact.
- Existing validation screenshots remain until fresh final validation screenshots are generated.
