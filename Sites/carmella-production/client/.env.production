# Production-only override: the public URL stays emenyu.com/Carmella/ (unchanged
# from the site this replaces) even though the internal restaurantId/tenant
# theme identity stays "carmella-production" (see RESTAURANT_ID below, kept
# constant across dev/prod so carmella-theme.css's selectors need no changes).
VITE_BASE_PATH=/Carmella
