# PostgreSQL / Prisma
# Do not commit a real URL. Keep the real value only in .env or your deployment secret store.
DATABASE_URL=postgresql://USER:emenyu123@HOST:5432/DATABASE?schema=public

# Production safety switches
NODE_ENV=production
ALLOW_LOCAL_DATABASE_URL=false
PRISMA_RUN_MIGRATIONS=false